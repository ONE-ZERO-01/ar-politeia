# AutoResearcher 框架设计图

> 用于审查框架合理性。所有图为 Mermaid，可在 Cursor / GitHub / VS Code 直接预览。
> 图例：矩形=Stage（LLM 做判断）｜菱形=分支决策｜双圈/红色=Gate（确定性代码，不可绕过）｜圆角=人类介入。

---

## 1. 总览：两循环 + 三 Gate + 人类边界

```mermaid
flowchart TD
    Q["question.md<br/>研究问题"] --> PLAN

    subgraph A["循环 A · 证据驱动"]
        direction TB
        PLAN["PLAN<br/>生成 claims[] + experiments[]<br/>→ plan.json"]
        EXPD["EXPERIMENT 设计<br/>baseline/ablation/指标/命令/资源"]
        REVIEWC["代码对齐审查<br/>→ review_log.json"]
        GATE1{{"GATE ① preflight<br/>commit/env/seed/artifact/策略"}}:::gate
        RUN["jobctl 提交/轮询/恢复<br/>→ jobs/*/manifest.json"]
        GATE2{{"GATE ② reconcile<br/>产物完整性核对"}}:::gate
        ANALYZE["ANALYZE<br/>判定 supported / contradicted / inconclusive<br/>→ findings.json"]

        PLAN --> EXPD --> REVIEWC --> GATE1
        GATE1 -- pass --> RUN --> GATE2 -- ok --> ANALYZE
        GATE1 -- blocked --> EXPD
        GATE2 -- 缺失/损坏 --> RUN
    end

    ANALYZE --> DA{"证据是否齐备?"}
    DA -- "有 unresolved / contradicted" --> STRAT_A["STRATEGY<br/>replan / revise / request_human"]
    STRAT_A -- replan（新 cycle）--> PLAN
    DA -- "全部 supported" --> WC

    subgraph B["循环 B · 论文与审稿"]
        direction TB
        WC["WRITE-COMPREHENSIVE<br/>完整长文（权威记录，无审稿）<br/>→ paper/comprehensive/"]
        WJ["WRITE-JOURNAL<br/>凝练投稿版<br/>→ paper/nature/、paper/prl/"]
        RV["REVIEW<br/>3 个隔离 Agent 模拟审稿<br/>科学性对照 findings.json<br/>→ reviews.json / improvement_plan.json"]
        WC --> WJ --> RV
    end

    RV --> DB{"STRATEGY 跳转"}
    DB -- "revise（叙事/格式）" --> WJ
    DB -- "replan（缺实验/方法缺陷）" --> PLAN
    DB -- "continue（全部通过）" --> DECIDE

    DECIDE["DECIDE<br/>→ decision_packet.json"]
    GATE3{{"GATE ③ audit<br/>证据链 / SHA-256 / claim 闭环<br/>→ reproducibility-bundle.json"}}:::gate
    DECIDE --> GATE3
    GATE3 -- "未通过" --> STRAT_A
    GATE3 -- "通过" --> HUMAN(["人类决策<br/>submit / revise / replan / terminate"]):::human

    classDef gate fill:#c0392b,stroke:#7b241c,color:#fff,font-weight:bold;
    classDef human fill:#f1c40f,stroke:#b7950b,color:#000,font-weight:bold;
```

---

## 2. 职责分层：谁做判断，谁做验证

```mermaid
flowchart LR
    subgraph L1["① LLM Agent（科学判断）"]
        direction TB
        a1["提出/收缩/重写 claim"]
        a2["设计实验、选算法、调参数"]
        a3["解释证据、判定结论"]
        a4["写论文、模拟审稿、给决策建议"]
    end

    subgraph L2["② Foundation 工具（确定性验证 · 不做科学判断）"]
        direction TB
        g1["preflight：复现前置约束"]
        g2["jobctl：幂等提交 / 崩溃恢复"]
        g3["audit：证据链完整性"]
    end

    subgraph L3["③ 人类（边界决策）"]
        direction TB
        h1["预算：token / GPU-hour / cycle"]
        h2["权限：账号 / 数据 / 主机 / 许可"]
        h3["发布：最终投稿 / 终止"]
    end

    L1 -- "产出 JSON 契约" --> L2
    L2 -- "pass=放行 / blocked=打回" --> L1
    L1 -- "request_human / stop_request" --> L3
    L3 -- "批准预算·权限·发布" --> L1

    classDef box fill:#ecf0f1,stroke:#95a5a6,color:#000;
    class L1,L2,L3 box;
```

---

## 3. 产物链（可复现的证据传递）

```mermaid
flowchart LR
    Q["question.md"] --> P["plan.json<br/>(claims + experiments)"]
    P --> J["jobs/*/manifest.json<br/>(artifacts + SHA + exit_code)"]
    J --> F["findings.json<br/>(supported/…)"]
    F --> C["paper/comprehensive/<br/>完整长文"]
    C --> JN["paper/&lt;journal&gt;/<br/>main.tex + claims.json"]
    JN --> R["reviews.json →<br/>improvement_plan.json"]
    R --> D["decision_packet.json"]
    D --> RB["reproducibility-bundle.json"]

    subgraph SIDE["贯穿全程"]
        S1["state.md（当前进度，可恢复）"]
        S2["versions/cycle-n/（历史归档）"]
    end

    classDef art fill:#d6eaf8,stroke:#2874a6,color:#000;
    class Q,P,J,F,C,JN,R,D,RB art;
```

audit 强制校验的三层 ID 闭环（任一断裂即 blocked）：

```mermaid
flowchart LR
    PC["plan.json<br/>claim.id"] <-- "必须一一对应" --> FC["findings.json<br/>claim_id"]
    FC <-- "paper claim 必须有 finding" --> PPC["paper claims.json<br/>claim_id → evidence[]"]
    PPC -- "evidence 文件" --> EV["存在 · 非空 · SHA 匹配 · 无 NaN/Inf · 相对路径"]

    classDef ok fill:#d5f5e3,stroke:#1e8449,color:#000;
    class PC,FC,PPC,EV ok;
```

---

## 4. 审查关注点（已知薄弱环节，标注在图上）

```mermaid
flowchart TD
    subgraph GATES["Gate 能验证的"]
        v1["证据文件存在/非空/SHA 匹配"]
        v2["claim→finding→paper 闭环"]
        v3["commit 非 dirty、seed 数量、exit_code=0"]
    end

    subgraph GAPS["Gate 验证不了的（审查重点）"]
        w1["实验设计是否有偏 / baseline 是否公平"]
        w2["统计检验是否恰当 / 结论是否过度外推"]
        w3["config 只查'存在任意 yaml/json'——可被空壳文件形式化满足"]
        w4["REVIEW 审稿 Agent 与写作 Agent 同源，有共同盲区"]
        w5["WRITE-COMPREHENSIVE 阶段无审稿"]
        w6["项目根在 OneDrive 同步盘，与 SHA 复现性目标冲突"]
    end

    GATES -->|"绿灯 ≠ 科学成立"| GAPS

    classDef good fill:#d5f5e3,stroke:#1e8449,color:#000;
    classDef bad fill:#fdebd0,stroke:#ca6f1e,color:#000;
    class v1,v2,v3 good;
    class w1,w2,w3,w4,w5,w6 bad;
```
